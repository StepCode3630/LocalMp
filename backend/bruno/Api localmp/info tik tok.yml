info:
  name: info tik tok
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/api/v1/playlistTikTok/:playlistId"
  params:
    - name: playlistId
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

info:
  name: signup
  type: http
  seq: 1

http:
  method: POST
  url: "{{baseUrl}}/api/v1/auth/signup"
  body:
    type: json
    data: |-
      {
        "fullName": "StepanPatricny",
        "email": "stepanpatricny@gmail.com",
        "password": "12345678",
        "passwordConfirmation": "12345678"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

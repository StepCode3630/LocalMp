info:
  name: log out
  type: http
  seq: 4

http:
  method: POST
  url: "{{baseUrl}}/api/v1/account/logout"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
